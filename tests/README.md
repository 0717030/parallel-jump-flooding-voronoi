# Tests

This folder is for future unit / regression tests.

Planned:
- `tests/data/` – small fixed seed sets and reference outputs.
- `test_cpu_jfa.cpp` – basic correctness tests (compare against exact Voronoi).
